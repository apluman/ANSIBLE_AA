- name: Run 'show system info' on all firewalls
  community.general.panos_op:
    ip_address: "{{ item }}"
    username: "{{ ansible_user }}"
    password: "{{ ansible_password }}"
    cmd: "<show><system><info></info></system></show>"
  loop: "{{ firewalls }}"
  register: results
